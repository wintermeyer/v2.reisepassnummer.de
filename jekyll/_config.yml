# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title:             Reisepassnummer
email:             stefan@wintermeyer.de
description:       "Reiseblog von Stefan Wintermeyer. Mit Mitte 40 nicht so hip und cool wie die jüngeren Blogger. Beruflich und privat auf Reisen. Mal mehr, mal weniger."
author:            Stefan Wintermeyer
baseurl:           ""
url:               "https://www.reisepassnummer.de"

# Social Profiles
twitter_username:  wintermeyer
github_username:   wintermeyer
facebook_username: stefan.wintermeyer

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# Collections
collections:
  ratgeber:
    output: true
  hotels:
    output: true

# Defaults
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: post
  - scope:
      path: ""
      type: "ratgeber"
    values:
      layout: post
  - scope:
      path: ""
      type: "hotels"
    values:
      layout: post
  - scope:
      path: "assets/imgages"
    values:
      image: true
